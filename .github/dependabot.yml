version: 2
updates:
  # Enable version updates for maven
  - package-ecosystem: "maven"
    # Look for maven in the `root` directory
    directory: "/"
    # Check for updates once weekly
    schedule:
      interval: "weekly"
